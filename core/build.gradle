// core/build.gradle
plugins { id 'java-library' }
group = 'org.egc'
version = '1.0'
dependencies {
    // vendor JARs — exposed via api so chexmix can use them directly
    api fileTree(dir: 'libs', include: ['**/*.jar'])
}
